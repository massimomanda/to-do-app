import { ThemetoggleDirective } from './themetoggle.directive';

describe('ThemetoggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemetoggleDirective();
    expect(directive).toBeTruthy();
  });
});
